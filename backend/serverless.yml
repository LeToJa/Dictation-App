service: dictation-backend

provider:
  name: aws
  stage: dev
  environment:
    DYNAMODB_ENDPOINT: http://localhost:8000
    SPEECHMATICS_API_KEY: ${env:SPEECHMATICS_API_KEY}
    SPEECHMATICS_BASE_URL: ${env:SPEECHMATICS_BASE_URL}
    SPEECHMATICS_LANG: ${env:SPEECHMATICS_LANG}

plugins:
  - serverless-offline
  - serverless-dynamodb

custom:
  serverless-offline:
    httpPort: 4000
    noPrependStageInUrl: true
    host: 0.0.0.0
  dynamodb:
    start:
      port: 8000
      inMemory: false
      dbPath: ./db
      migrate: true

functions:
  register:
    handler: src/functions/register.handler
    events:
      - http:
          path: register
          method: post
          cors: true
  login:
    handler: src/functions/login.handler
    events:
      - http:
          path: login
          method: post
          cors: true
  list:
    handler: src/functions/file/list.handler
    events:
      - http:
          path: files
          method: get
          cors: true
  upload:
    handler: src/functions/file/upload.handler
    events:
      - http:
          path: files/upload
          method: post
          cors: true
  get:
    handler: src/functions/file/get.handler
    events:
      - http:
          path: files/get/{fileId}
          method: get
          cors: true
  delete:
    handler: src/functions/file/delete.handler
    events:
      - http:
          path: files/delete/{id}
          method: delete
          cors: true
  transcribe:
    handler: src/functions/file/transcribe.handler
    timeout: 600
    events:
      - http:
          path: files/transcribe/{id}
          method: post
          cors: true

resources:
  Resources:
    UsersTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: dictation-users
        AttributeDefinitions:
          - AttributeName: id
            AttributeType: S
        KeySchema:
          - AttributeName: id
            KeyType: HASH
    FilesTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: dictation-files
        AttributeDefinitions:
          - AttributeName: id
            AttributeType: S
          - AttributeName: userId
            AttributeType: S
        KeySchema:
          - AttributeName: id
            KeyType: HASH
        GlobalSecondaryIndexes:
          - IndexName: UserIdIndex
            KeySchema:
              - AttributeName: userId
                KeyType: HASH
            Projection:
              ProjectionType: ALL
